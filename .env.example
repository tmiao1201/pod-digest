# pod-digest 环境配置说明（本 skill 设计为零自有 key）
#
# 唯一可能需要的是转写引擎的 key，两种方式二选一：
#
# 方式A（推荐，本地）: 委托 agent-reach 管理
#   1. 免费注册 https://console.groq.com/keys
#   2. agent-reach configure groq-key <你的key>
#   3. agent-reach install --env=auto
#   （本地私设细节见 references/local-setup.md）
#
# 方式B（公域/最小依赖）: 直接环境变量
#   export GROQ_API_KEY=gsk_xxx
#
# 不配 key 也能用: 仅字幕路径（YouTube 字幕）可用，纯音频播客会明确提示需要 key。
# 字幕优先的设计下，有视频版的节目永远不需要 key。

# GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxx
